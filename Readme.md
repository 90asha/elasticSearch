To run this project via Docker run following command:
# sudo docker build -f Dockerfile -t springapp .
# sudo docker run -p 8080:8080 springapp

To run this project via spring boot:
# ./gradlew bootrun
